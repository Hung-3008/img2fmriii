# =============================================================================
# FactFlow for fMRI Synthesis — Subject 1  [EXP 1a: source-encoder ON]
# CLIP image features → PerceiverVE (learned x0 from 256 tokens) → Flow Matching → fMRI
#
# vs baseline (factflow_fmri.yaml): baseline conditions the DiT ONLY on the
# 1280-d pooled CLIP vector via AdaLN (c = t + y), leaving the 256 rich CLIP
# tokens unused → val voxel_r plateaus ~0.14. This config routes the 256 tokens
# through PerceiverVE to form an image-specific source x0, so the flow transports
# x0 → fMRI from an informative starting point instead of pure noise.
# =============================================================================

# --- Source encoder toggle (true = learned x0 from CLIP tokens) ---
use_source_encoder: true

# --- Data ---
data:
  data_dir: NSD/data/nsd
  subject: 1
  fmri_mode: zscore          # "scale" or "zscore"
  clip_feature: sdxl_clip   # feature prefix
  n_voxels: 15724           # raw voxel count for subj01
  pad_to: 16384             # padded length (must be divisible by patch_size)
  avg_reps: true            # average 3 reps per image -> 9000 train samples (reduces session label-noise)
  # 1D mode — no fmri_channels / fmri_spatial needed

# --- Stage 2: Velocity model (DiT1D — native 1D) ---
stage_2:
  target: model.dit_1d.DiT1D
  params:
    seq_len: 16384           # = pad_to
    patch_size: 32           # 16384/32 = 512 tokens
    in_channels: 1           # fMRI as single-channel 1D signal
    hidden_size: 512
    depth: 12
    num_heads: 8
    mlp_ratio: 4.0
    y_in_channels: 1280     # SDXL CLIP pool dim
    learn_sigma: false
    use_qknorm: true
    use_swiglu: true
    use_rmsnorm: true
    use_rope: true
    wo_shift: false

# --- Source encoder (PerceiverVE): CLIP tokens → learned x₀ ---
# Output: (B, 256, 64) → permute+reshape → (B, 4, 64, 64) = 16384 values
source_encoder:
  target: model.perceiver.PerceiverVE
  params:
    in_channels: 1664       # SDXL CLIP token dim
    out_channels: 64        # 256 queries × 64 = 16384 = pad_to
    hidden_size: 384
    depth: 4
    num_heads: 8
    mlp_ratio: 4.0
    dropout_prob: 0.0
    num_queries: 256         # 16×16 grid; 256 × 64 = 16384
    max_input_len: 256       # CLIP token count
    pos_emb_type: learned
    use_qknorm: false
    use_swiglu: true
    use_rmsnorm: true
    use_variational: true
    init_logvar: 0.0
    fixed_std: 1.0

# --- Transport (Flow Matching) ---
transport:
  params:
    path_type: Linear
    prediction: velocity
    loss_weight: null
    time_dist_type: "logit-normal_-1.0_1.0"   # focus on t~0.1-0.4 (weak zone)
    time_dist_shift: 16384   # = dim → shift=sqrt(16384/16384)=1.0 (no extra warp)

# --- ODE sampler ---
sampler:
  mode: ODE
  params:
    sampling_method: euler
    num_steps: 50
    atol: 1.0e-06
    rtol: 0.001
    reverse: false

# --- Losses ---
# align_loss shapes the source mean toward the target so x0 is informative.
# KLD stays off: fixed_std=1.0 means variance is constant (not learnable), so
# the KL term carries no useful gradient here.
losses:
  use_kld_loss: false
  kld_loss_type: var_kld
  kld_loss_weight: 5.0
  kld_target_std: 1.0
  kld_reduction: mean
  use_align_loss: true
  align_loss_weight: 1.0
  align_loss_type: normalized_l2
  detach_ut: false

# --- Training ---
training:
  global_seed: 42
  epochs: 200
  batch_size: 64
  grad_accum_steps: 8       # effective batch = 8 × 8 = 64
  num_workers: 4
  log_every: 50
  ckpt_every: 5000
  sample_every: 2000
  val_every: 5
  optimizer:
    lr: 2.0e-04
    betas: [0.9, 0.95]
    weight_decay: 0.01
  base_lr: 2.0e-04
  final_lr: 2.0e-05
  schedule_type: cosine
  warmup_steps: 500
  clip_grad: 1.0
  precision: bf16
